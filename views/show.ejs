<!DOCTYPE html>
<html lang="en">
<%- include('./partials/header.ejs') %>
  <body>
    <%- include ('./partials/nav.ejs') %>
    <h1><%= product.name %></h1>
    <h2>How I felt about this product</h2>
    <p><%= product.description %></p>
    <% if (product.finishedProduct === true) { %>
    <p>I finished this product</p>
    <% } else { %>
    <p>I did not finish this product</p>
    <% } %> <% if (product.timeLasted === true) { %>
    <p>I finished this product in <%= product.timeLasted %></p>
    <% } %> <% if (product.addToFavorites === true) { %>
    <p>star image placeholder</p>
    <% } %>
  </body>
</html>
